# Quickstart

Run the packaged example workflow:

```bash
isotomics-quickstart
```

This command uses bundled example inputs under `isotomics/input_data/Experimental_Data/` and writes outputs in your current working directory.

Programmatic entry point:

```bash
python -m isotomics
```
